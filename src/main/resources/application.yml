spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/employee-sharok
    username: postgres
    password: aswa19
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

server:
  port:
    8090